<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.mapper.UserMapper">
    <select id="getPointObject" resultType="com.course.pojo.PointObject">
        select * from `pointobject`
    </select>
    <select id="getPointObjectByID" parameterType="int" resultType="com.course.pojo.PointObject">
        select * from `pointobject` where id = #{id}
    </select>
    <!--对象中的属性可以直接取出-->
    <insert id="addPointObject" parameterType="com.course.pojo.PointObject">
        insert into `pointobject`(id, growScore, exchangeScore, scoreTotal, lastComplicationTime, lastLoginTime, lastYdqnTime, profileInputed, bloodSugarCount)
        values (#{id},#{growScore},#{exchangeScore},#{scoreTotal},#{lastComplicationTime},#{lastLoginTime},#{lastYdqnTime},#{profileInputed},#{bloodSugarCount})
    </insert>
    <update id="updatePointObject" parameterType="com.course.pojo.PointObject">
        update `pointobject`
        set growScore=#{growScore}, exchangeScore=#{exchangeScore}, scoreTotal=#{scoreTotal},
            lastComplicationTime=#{lastComplicationTime}, lastLoginTime=#{lastLoginTime},
            lastYdqnTime=#{lastYdqnTime}, profileInputed=#{profileInputed}, bloodSugarCount=#{bloodSugarCount}
        where id=#{id}
    </update>
    <delete id="deletePointObject" parameterType="int">
        delete from `pointobject` where id=#{id}
    </delete>
</mapper>